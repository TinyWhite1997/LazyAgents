{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SessionsReplayParams",
  "type": "object",
  "required": [
    "from_seq",
    "session_id"
  ],
  "properties": {
    "session_id": {
      "type": "string"
    },
    "from_seq": {
      "description": "Inclusive starting sequence number for the replay.",
      "type": "integer",
      "format": "uint64",
      "minimum": 0.0
    },
    "max_bytes": {
      "description": "Optional hard cap on the bytes the daemon will resend before switching back to live streaming. `None` ⇒ daemon default.",
      "type": [
        "integer",
        "null"
      ],
      "format": "uint64",
      "minimum": 0.0
    }
  }
}
