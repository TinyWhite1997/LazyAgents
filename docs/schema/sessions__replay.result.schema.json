{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SessionsReplayResult",
  "type": "object",
  "required": [
    "bytes_queued",
    "last_seq"
  ],
  "properties": {
    "last_seq": {
      "description": "Last `seq` covered by the replay. If equal to `from_seq - 1`, the ring buffer no longer contained anything for that range.",
      "type": "integer",
      "format": "uint64",
      "minimum": 0.0
    },
    "bytes_queued": {
      "description": "Total bytes about to be (re)delivered as `session.output` notifications. Lets the client budget UI updates.",
      "type": "integer",
      "format": "uint64",
      "minimum": 0.0
    }
  }
}
