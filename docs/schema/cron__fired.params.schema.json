{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CronFiredParams",
  "type": "object",
  "required": [
    "cron_id",
    "fired_at",
    "run_id",
    "status"
  ],
  "properties": {
    "cron_id": {
      "type": "string"
    },
    "run_id": {
      "type": "string"
    },
    "fired_at": {
      "description": "RFC3339 timestamp of the firing.",
      "type": "string"
    },
    "status": {
      "description": "Status of the run at the moment this notification is emitted (most commonly `\"spawning\"` or `\"running\"`). Use the full `runs.list` for terminal status — this is the kick-off pulse.",
      "type": "string"
    }
  }
}
